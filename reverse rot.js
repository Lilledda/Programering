const readline = require ('')

rl.question('type in a number and a message: ',(awnser) => {

    let splitawnser = answer.split (' ')
let rotation = number (splitawnser [0])
let message = splitawnser [1]

for (const letter in< object) {
    if (object.hasOwnProperty(letter)) <{
        const element = object[letter];
<        
    }
}
let letterindex =

}  